# Used by infra/docker-compose.yml for the postgres service.
# The API's own secrets (JWT key, provider API keys, ...) live in apps/api/.env - see
# apps/api/.env.example.
POSTGRES_USER=futurist
POSTGRES_PASSWORD=futurist
POSTGRES_DB=futurist_os
